Setup vim

Requires Vim 8+

Symlink .vimrc to ~

Install Vundle by running

$ git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

Install ctags:

brew install ctags

Open vim and run

:PluginInstall 

